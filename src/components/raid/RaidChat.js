import React, { Component } from 'react';
import { FormGroup, Button, FormControl } from "react-bootstrap";

export default class RaidChat extends Component {
    render() {
        return (
            <div className="raid-chat"></div>
        );
    }
}

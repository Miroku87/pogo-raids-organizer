import React, { Component } from 'react';
import { FormGroup, Button, FormControl } from "react-bootstrap";

export default class RaidInfo extends Component {
    render() {
        return (
            <div className="raid-info"></div>
        );
    }
}
